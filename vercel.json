{
    "buildCommand": "prisma generate && next build",
    "ignoreCommand": "echo 'No need to ignore anything'",
    "functions": {
      "api/**/*.ts": {
        "memory": 1024,
        "maxDuration": 10
      }
    }
  }
  